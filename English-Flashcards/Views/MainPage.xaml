<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="English_Flashcards.MainPage"
             xmlns:viewmodels="clr-namespace:English_Flashcards.ViewModels"
             xmlns:models="clr-namespace:English_Flashcards.Models"
             Title="{Binding Title}"
             >
    <ContentPage.BindingContext>
        <viewmodels:MainPageVewModel />
    </ContentPage.BindingContext>
    <Grid BindableLayout.ItemsSource="{Binding Cards}">
        <BindableLayout.ItemTemplate>
            <DataTemplate>
                <Grid>
                    <SwipeView>
                        <SwipeView.LeftItems>
                            <SwipeItems Mode="Execute">
                                <SwipeItem Text="Удалить" BackgroundColor="Gold"/>
                            </SwipeItems>
                        </SwipeView.LeftItems>
                        <StackLayout Margin="{Binding DisplayOptions.Margin}"
                              WidthRequest="300"
                              HeightRequest="300"
                              HorizontalOptions="CenterAndExpand"
                              VerticalOptions="CenterAndExpand"
                              BackgroundColor="{Binding DisplayOptions.BackColor}" 
                            >
                            <Label Text="{Binding RussianText}" ZIndex="{Binding DisplayOptions.ZIndex}" 
                                   HorizontalOptions="CenterAndExpand"
                                   VerticalOptions="CenterAndExpand"
                                   HorizontalTextAlignment="Center"
                                   />
                        </StackLayout>
                    </SwipeView>
                </Grid>
            </DataTemplate>
        </BindableLayout.ItemTemplate>
    </Grid>
</ContentPage>
